import React from 'react';
import {Container} from 'native-base';
import {AppContent} from '../components/Content';

export const JokesTab = () => {
  return (
    <Container>
      <AppContent />
    </Container>
  );
};
